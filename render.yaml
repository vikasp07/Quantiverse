services:
  # Frontend Web Service
  - type: web
    name: quantiverse-frontend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist -l $PORT
    envVars:
      - key: VITE_API_BASE_URL
        value: https://quantiverse-backend.onrender.com